--- a/testing/cover
+++ b/testing/cover.go
@@ -108,5 +108,5 @@ func coverReport() {
 	if total == 0 {
 		total = 1
 	}
-	fmt.Printf("coverage: %.1f%% of statements%s\n", 100*float64(active)/float64(total), cover.CoveredPackages)
+	fmt.Printf("coverage: %.1f%% of %v statements%s\n", 100*float64(active)/float64(total), total,  cover.CoveredPackages)
 }

